{% extends 'layout.njk' %}

{% block styles %}
    <link rel="stylesheet" href="/styles/list.css">
    <link rel="stylesheet" href="/styles/presentation.css">
    <link rel="stylesheet" href="./styles/styles.css">
{% endblock %}


{% block content %}

    <div class="info">

    </div>


    <div class="content">
        <header>
            <a href="/register">Cadastro</a>
            <a href="/login">Login</a>
        </header>

        <div class="changeSchool">
            <a href="/teachers">Professores</a>
            <a href="/students">Alunos</a>
        </div>

        <div class="welcome">
            <h2>Consulte os Professores</h2>
        </div>

        <div class="table-container">
            <table>
                <thead> {# Cabeçalho #}
                    <tr> {# Linha do Cabeçalho #}
                        <th>Professores</th>
                        <th>Acompanhamento</th>
                        <th>Ação</th>
                    </tr>
                </thead>
                <tbody>
                    {% for teacher in teachers %}
                        <tr>
                            <td>
                                <span style="background-image: url({{teacher.avatar_url}})"></span>
                                {{ teacher.name }}
                            </td>
                            <td>
                                {% for plus in teacher.plus %}
                                    <span class="list-plus">{{ plus }}</span>
                                {% endfor %}
                            </td>
                            <td><a href="/register/{{teacher.id}}">ver</a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>


    </div>


    <div class="info">

    </div>


{% endblock %}